import styled from 'styled-components'

export const ManageRateContainer = styled.div `
    width: 100%;
    height: calc(100vh - 200px);

    .body {
        background-color: #fff;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        padding: 40px 20px;
    }
`